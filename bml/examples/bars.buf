# invert each pixel

macro add_one(a) a + 1
macro neg(a) 0 - a

uv = coord / resolution

return [1; 4] if mod(floor(uv.x * 200), 2) == 0 else [0, 0, 0, 1]

# uv = coord / resolution

# return [1 - frag.r, 1 - frag.g, 1 - frag.b, 1] if 1 else { give 0.5 }