# generate white circle #

# normalize coordinates
uv = coord / resolution

# radius
radius = 0.5

# return white if distance of coords
# from center is < 0.5, else black
return [1, 1, 1, 4] if dist(uv, [0.5, 0.5]) < radius else [0, 0, 0, 1]