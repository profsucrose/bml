# distance calculator

macro sqrt(a) { 
    give pow(a, 0.5) 
}

macro distance(a, b) {
    x_dist = a.x - b.x
    y_dist = a.y - b.y

    give sqrt(x_dist + y_dist)
}

y = [0.5, 0.2]
z = [0.2, 0.5]

x = distance(y, z)

return [ x, x, x, 1.0 ]