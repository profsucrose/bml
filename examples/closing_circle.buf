# draw black vignette that gets smaller each frame

macro PI() 3.14159
macro mult(a, b) a * PI()

return mult(3, 3)

# uv = coord / resolution

# return frag if dist(uv, [0.5, 0.5]) < (frame / max_frame) else [0; 4]